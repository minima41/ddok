# 변경 이력

## 1.2.0

- 예약 대상을 `이도아` 또는 `이도연` 중 앱 화면에서 선택
- 희망 시간을 1~5순위 드롭다운으로 직접 지정
- 기본값을 `09:10 → 09:20 → 09:30`으로 설정
- `{{PATIENT}}`, `{{TIME_PRIORITIES}}` 동적 템플릿 변수 추가
- v1.1의 하드코딩된 환자명·시간 목록도 실행 직전에 자동 교체
- 상태 화면에 현재 예약 대상과 시간 시도 순서 표시

## 1.1.0

- 시간 우선순위 예약 상태 머신 추가
- 앞 시간이 선점되면 실패 팝업을 닫고 다음 시간으로 자동 이동
- 테스트 모드에서 최종 예약 버튼 앞에 정지
